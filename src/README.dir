This directory contains the core source code of the program 'ehdd'.

The file 'main.c' is compiled into binary. The other ('*.h') header files are include by 'main.c', each containing functions and definitions required by 'main.c'.
