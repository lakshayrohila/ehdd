This directory contains helper scripts which run at build-time, both locally (using make) and through GitHub workflows (official releases). Thus, the directory is also sub-divided into two:-
	1. local
	2. github
